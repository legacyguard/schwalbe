import React from 'react';
export const DropdownMenu = ({ children }) => React.createElement('div', null, children);
export const DropdownMenuTrigger = ({ children }) => React.createElement('button', null, children);
export const DropdownMenuContent = ({ children }) => React.createElement('div', null, children);
export const DropdownMenuItem = ({ children }) => React.createElement('div', null, children);
