import React from 'react';
export const Tabs = ({ children }) => React.createElement('div', null, children);
export const TabsList = ({ children }) => React.createElement('div', null, children);
export const TabsTrigger = ({ children }) => React.createElement('button', null, children);
export const TabsContent = ({ children }) => React.createElement('div', null, children);
